PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT DISTINCT ?license WHERE {
  VALUES ?vocab { <__VOCAB__> }   # esto lo suele rellenar tu runner
  GRAPH <http://lov_server:3333/dataset/lov> {
    ?vocab dcterms:license ?license .
  }
}
